<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.groupprojectdashboard">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroupProjectDashboard"
        tools:targetApi="31">

        <!-- Main launcher: Dashboard -->
        <activity
            android:name="com.example.groupprojectdashboard.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activities used in Dashboard -->
        <activity android:name="com.example.groupprojectdashboard.TransferActivity" android:exported="false" />
        <activity android:name="com.example.groupprojectdashboard.BorrowActivity" android:exported="false" />
        <activity android:name="com.example.groupprojectdashboard.PendingActivity" android:exported="false" />
        <activity android:name="com.example.groupprojectdashboard.ApproveActivity" android:exported="false" />

    </application>

</manifest>
